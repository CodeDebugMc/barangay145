import React from "react";
import ReactDOM from "react-dom/client";
import CertificationAction from "./components/CertificationAction"; // adjust path

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <CertificationAction />
  </React.StrictMode>
);
